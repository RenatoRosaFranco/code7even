<%= form_with(model: dashboard_newsletter, local: true) do |form| %>
  <% if dashboard_newsletter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard_newsletter.errors.count, "error") %> prohibited this dashboard_newsletter from being saved:</h2>

      <ul>
      <% dashboard_newsletter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :dashboard_newsletter_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :dashboard_newsletter_email %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status, id: :dashboard_newsletter_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
